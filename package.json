{
  "name": "girlycoded-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:zip": "npm run build && cd dist && bestzip dist.zip *"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "bestzip": "^2.2.2",
    "marked": "^17.0.4",
    "vite-plugin-static-copy": "^3.3.0"
  }
}
